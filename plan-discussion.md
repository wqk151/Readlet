# 待确认方案（9 项 todo 中 3 项需你拍板）

请在对应选项后写 **选中**（或直接改文字），其余已定方案可直接开工。

## 已定（无需讨论，根因已用真机数据验证）

| # | 问题 | 方案 |
|---|---|---|
| 1 | 搜索命中排序后定位在老位置 | 搜索/筛选变化时 LazyColumn `scrollToItem(0)` 回顶 |
| 2 | 卡片库无滚动条 | Compose `VerticalScrollbar` 原生实现 |
| 3 | 难点词逗号粘连、点进去不高亮 | 入库/统计时按 `, 、 ; /` 拆分重聚合（真机数据：`bottle fame, brew glory, stopper death` 等 2 条；不高亮因存储串≠句中原文） |
| 5 | 显示数量 + 序号 | 卡片库头部「共 N 条」；每卡全局序号（按收藏时间倒序排名），卡片库/复习显示同一编号可互相对照 |
| 6 | 复习界面字体跳变 | 根因：首帧队列未加载完先显示"今日复习完成"空态（默认字体）→ 衬线句子。修：加"加载中"态 + 重点词预载，首帧即正确渲染 |
| 8 | 模糊查询命中无关卡 | 根因：fzf 式**跨词子序列**匹配（`couple` 命中 12/19 卡，无一含该词）。改词级匹配：整词=100 / 词首=60 / 词内包含=30，词组按整短语匹配，不再跨词 |

## 待确认

### Q1 · #4 六级/考研/雅思/专四/专八词自动标记为重点词

**背景**：词表源已全部验证可用（六级 5651 + 考研 9602 + 雅思 3427 + 专四约 9k + 专八约 2k，共约 300KB）。

- **A. 本地词表（推荐）**：打包 5 份词表为 assets；分析时对句子分词做 HashSet 查表，O(1) 每词、零 LLM 成本、即时且确定；卡上显示「六级」等角标。
- **B. 纯 LLM 提取**：prompt 加一句「重点词优先包含这些级别词」。零数据资产，但级别判断靠模型记忆、不稳定，且 2-4 个关键词上限会挤占真正值得学的表达。
- **C. 混合**：词表兜底 + prompt 提示。覆盖最全，成本略高。

我的建议：**A**（B 的不确定性不符合"学习工具"定位；C 的收益小于复杂度）。

> 确认使用A，本地有个词库你可以参考，看看是否有用：/home/wqk/.cache/kdcache/kd_data.db ，不强制，有用则用
### Q2 · #7 学习流程设计

**现状**：分享 → 新卡自动进复习队列（每日 10 张上限）；卡片库点开详情可学。割裂点：没学过的卡也会出现在复习里。

- **A. 显式学习门槛（推荐）**：分享后卡片标「待学习」；打开详情页学习（译文/重点词/语法），点「开始学习」才进入复习排程（次日到期）。闭环：采集 → 学习 → 复习 → 统计。旧数据迁移为已学习。
- **B. 保持自动进队 + 加强入口**：新卡仍自动进队列，卡片库每行加「去学习」直达详情；复习首见即现场学。改动最小。
- **C. 仅改文案引导**：详情页标题加「学习笔记」、新卡角标「新」。排程逻辑不动。

我的建议：**A**（把"学习"变成显式动作，流程才有闭环感；B 治标）。

> 好的按你推荐，不过先更新到ui-design.html之后，我确认下看看效果再决定是否进行下一步
### Q3 · #9 翻译术语表（你说优先级不高）

- **A. 全局术语表（推荐）**：设置里加多行术语表（如 `tolerance→公差`、`clearance→间隙`），分析时注入 prompt「遇到以下术语必须按指定译法」。对所有来源生效，改动小。
- **B. 按来源/书配置**：术语表按书名/来源分组，分享时按 source 匹配注入。更贴合场景但需要来源管理 UI，复杂度明显上升。
- **C. 本期不做**：记入 v1.5 预留。

我的建议：**A**（满足核心诉求；B 等真有多个专业领域需求再做）。

> 好的按你的推荐来做

## 确认后流程

1. 你在此文件答复 → 2. 更新 `DEVELOPMENT.md`（+`DESIGN.md` 数据模型）→ 3. 逐项实现 → 4. `assembleDebug` + adb 真机验证。
