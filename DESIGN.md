# 拾句 Readlet · 设计文档

> 阅读时随手拾句，在句子语境中积累词汇。
> 状态：v1 设计定稿 · UI 原型见 `ui-design.html`

## 1. 产品定位

**一句话**：用手机读英文文章/小说时，把想学的句子分享进来，手动分析成可复习的卡片，用间隔重复记住它——学的是"句子在语境里的用法"，不是孤立背单词。

**核心原则**（一切设计决策的判据）：

1. **句子是主体，词是索引**。复习单位是句子；单词只是句子上的标注，通过词频反向索引句子。
2. **语境释义**。单词释义只显示"在本句中的含义"（语境义），不是词典全义列表。全义列表可折叠展开（v1.5）。
3. **离线优先**。采集永远可用；分析分本地即时层与 LLM 增强层，离线时降级。
4. **闭环**：采集 → 手动分析 → 复习 → 统计；收藏只入库不消耗 token，分析由用户按需触发（一键分析/详情页按钮）。

## 2. 用户流程

```
在其他 App 阅读 → 选中句子 → 系统分享 → 拾句
   │
   ├─ 立即（本地）: 入库（待分析）、高亮提示；不自动分析、不打断阅读
   ├─ 手动（LLM）: 卡片库「一键分析」/ 详情页「开始分析」
   │     └─ 断网 → 卡片保持"待分析"，联网后手动补
   └─ 复习（SM-2）→ 统计反馈（日历/曲线/词频加练）
```

## 3. 功能范围

### v1（本期）
| 模块 | 内容 |
|---|---|
| 采集 | `ACTION_SEND`（分享文本）+ `ACTION_PROCESS_TEXT`（选中文本→分享到）；来源（书名/标题优先，降级 App 名）与时间记录；多段落分享保留段落 |
| 分析 | LLM（DeepSeek，OpenAI 兼容协议）结构化输出：翻译/重点词(含音标词性语境义)/句法/搭配；JSON 容错与重试；断网挂起 |
| 卡片 | 收件箱（三态：待分析/分析中/已分析）、卡片库（搜索/筛选/复习次数/待学习·已掌握计数） |
| 复习 | SM-2 间隔重复，四键评级（重来/困难/良好/简单），学习门槛（待学习→开始学习，次日排程），**加练模式**（不计入排程） |
| 统计 | 月度概览、GitHub 式热力图（12 周×7 天，月份/星期轴、今日标记、图例）、近 30 天复习量曲线、累计卡片曲线、**高频难点词榜→句子加练** |
| 设置 | API Key / Base URL / 模型名（SharedPreferences） |

### v1.5（预留）
- 本地 ECDICT 词典（离线即时词义 + 词频判定），接口 `DictionarySource` 已预留
- FSRS 替换 SM-2（SrsEngine 接口）
- 卡片导入/导出（Anki 兼容）、全义列表折叠

### v2（不做）
- 无障碍悬浮窗自动采集（跨 App 翻译式）
- 记忆保持率预测曲线、多设备云同步、登录

## 4. UI 设计

### 4.1 结构
- 底部导航 4 Tab：**收件箱 / 卡片库 / 复习 / 统计**（`ui-design.html` 原型）
- 全屏覆盖页 2 个：卡片详情（收件箱进入）、词频详情（统计进入，含"加入加练"）
- 设置：收件箱头部齿轮 → 对话框

### 4.2 设计 Token
| Token | 值 | 用途 |
|---|---|---|
| 暖纸底 | `#F6F3EC` | 全局背景（阅读感，非纯白） |
| 主绿 | `#2F6F4F` | 强调、已分析、主按钮 |
| 琥珀 | `#B07D2B` | 打卡、困难评级 |
| 红 | `#B0524A` | 重来评级、待分析警示 |
| 蓝 | `#3B6EA5` | 分析中、简单评级 |
| 淡蓝 | `#7BA6CE` | 词根词缀（构词）文字 |
| 句字体 | Georgia / serif | 句子正文（西文衬线更"阅读"） |
| 卡片 | 白底 + `#E8E3D8` 描边 + 圆角 16dp | 一致卡片语言 |

### 4.3 关键交互
- 卡片状态四态色：已分析=绿、分析中=蓝、待分析=红、待学习=琥珀
- **学习门槛（方案 A）**：分享后卡片标「待学习」，不进复习队列；详情页点「开始学习」→ 次日到期进入排程；未学习的卡不会出现在复习里
- 复习：正面句子（重点词高亮）→ 显示答案 → 四键评级；失败（重来）重置进度、当天不再出
- **加练**：词频榜点词 → 该词相关句子列表 → "加入加练"；加练复习不写 SRS 字段，只记日志
- 分享进来只收藏不自动分析（不打断阅读）：卡片置「待分析」，卡片库「一键分析」批量补、详情页「开始分析」单张触发；失败可重试
- 「一键分析」并发 5 并行，按钮显示「分析中 done/total」；卡片库筛选 chip「待学习/已掌握」显示计数（>0 时）
- 卡片库列表与复习页的「已学次数」改为按复习日志统计的「已复习 N 次」（SM-2 reps 不再对外展示）
- **固定编号（v0.5）**：每张卡有永久编号 = `Card.id`（AUTOINCREMENT 永不复用，备份恢复后不变）；卡片库行头 / 复习页 / 详情页底部统一显示 `#编号`；搜索框输入纯数字直接定位到该卡（未命中显示专属空态）。取代 v0.4 的「收藏时间倒序排名」全局序号（随增删变化，不可作稳定引用）

## 5. 数据模型

```
Card (句子卡片) 1 ──── n CardWord (重点词标注)
  │                      （同词跨卡 = 词频索引）
  1
  n
ReviewLog (复习日志，SRS 排程与统计的数据源)

LlmUsage (大模型调用记录，独立于卡片，统计「调用次数 / Token 用量」)
```

### Card
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Long | PK （对外固定编号，AUTOINCREMENT 永不复用）|
| text | String | 句子原文（分享文本） |
| source | String | 来源 App（`getCallingPackage` 解析或显示名） |
| status | Int | 0 待分析 / 1 分析中 / 2 已分析 / 3 失败 |
| translation | String? | 整句译文 |
| pointsJson | String? | 重点表达列表（LLM JSON 原文存储） |
| grammarJson | String? | 语法分析列表 |
| collocationsJson | String? | 搭配积累列表 |
| createdAt | Long | 采集时间 ms |
| analyzedAt | Long? | 分析完成时间 |
| ef | Double | SM-2 难度系数，初始 2.5 |
| interval | Int | 间隔天数 |
| reps | Int | 成功次数 |
| lapses | Int | 失败次数 |
| dueAt | Long | 到期日 epochDay；null=待学习（未点「开始学习」，不进复习队列） |

### CardWord
| 字段 | 说明 |
|---|---|
| id / cardId | PK / FK |
| word | 词形（**索引键**，GROUP BY 得词频） |
| phonetic / phoneticUs | 英式音标 / 美式音标（v6 新增美音列；LLM 优先、词表兜底，美音词表无数据，缺失不显示） |
| pos | 词性 |
| meaningInContext | **语境释义**（本句中的含义） |
| level | 考试级别角标（v4 新增，v6 起 LLM 提供、词表兜底）：六级/考研/雅思/专四/专八，空 = 无级别 |
| lemma | 原型（v6 新增）：LLM 提供、词表变形还原兜底；原形或词组为 null |
| affix | 词根词缀拆解 JSON（v7 新增）：构件数组 [part,type,meaning]，仅 LLM 重点词提供；无法确定为空 |
| orderIdx | 卡内显示顺序 |

### ReviewLog
| 字段 | 说明 |
|---|---|
| id / cardId | PK / FK |
| grade | 1/3/4/5（SM-2 quality 映射） |
| type | 0 排程复习 / 1 加练 |
| reviewedAt | ms |
| reviewedDay | epochDay（热力图/曲线聚合键） |
| intervalAfter | 本次后的间隔 |

### LlmUsage（v3 新增）
| 字段 | 说明 |
|---|---|
| id | PK |
| calledAt | 调用时间 ms |
| promptTokens / completionTokens / totalTokens | 本次调用的 token 用量（服务商不返回时记 0） |

一次成功调用一行（调用已产生费用即记账，后续解析失败也计入）。统计数据全部由以上表派生，零额外采集：
- 打卡/热力图/复习量曲线：`reviewlog GROUP BY reviewedDay`
- 累计卡片：`cards GROUP BY createdAt 日`
- 词频榜：`cardword` 全量拉取 → Kotlin 侧按分隔符拆分重聚合（LLM 偶发把并列结构合并成一个关键词，如 `bottle fame, brew glory, stopper death`；拆分后每个分项独立成词，点击必能在句中高亮），展示"出现 N 次 · M 句"
- 词/词组（原"已掌握单词"）：已分析卡片中出现过的去重词数
- 大模型调用次数 / Token 用量：`llm_usage COUNT(*) / SUM(totalTokens)`

## 6. LLM 分析设计

### 6.1 Prompt 资产
`assets/analyze_prompt.txt`：以用户提供的完整学习助手 prompt 为底稿（输入类型识别/单词短语模式/句子模式/错误处理/输出约束），**追加"JSON 应用输出约束"一节**（见下）。v7 起追加：学习者水平基线（大一 ≈ 四级，四级以下视为已掌握）、keywords 上限 4-6 且超纲必挑、affix 字段约束。

### 6.2 JSON Schema（v1 结构化输出）
```json
{
  "mode": "sentence | word | phrase",
  "translation": "整句译文（单词/短语模式为 null）",
  "keywords": [
    { "word": "muffling", "phonetic_uk": "/ˈmʌflɪŋ/", "phonetic_us": "/ˈmʌflɪŋ/",
      "pos": "v.", "level": "六级", "lemma": "muffle",
      "affix": [
        { "part": "muffle", "type": "词根", "meaning": "捂住（声音）" },
        { "part": "-ing", "type": "后缀", "meaning": "现在分词，表示进行中" }
      ],
      "meaning_in_context": "使(声音/光线)变低沉；此处指雾气模糊了灯光" }
  ],
  "points": [
    { "expr": "muffling the harbor lights",
      "meaning": "现在分词伴随状语：压低/模糊了港口灯火" }
  ],
  "grammar": [
    { "structure": "The fog rolled in",
      "explanation": "主干：主语 + 谓语（不及物短语动词）" }
  ],
  "collocations": [
    { "phrase": "roll in", "meaning": "(雾/云)滚滚而来",
      "example": "Dark clouds rolled in from the west.",
      "example_cn": "乌云从西边滚滚而来。" }
  ]
}
```
映射关系：keywords→卡片高亮+词卡；points→详情"要点"；grammar→详情"语法分析"；collocations→详情"搭配积累"；affix→词元信息行（构词）。全部按用户 prompt 的"只解释与本句相关含义"约束。

### 6.3 容错链
1. 响应清理（剥代码围栏 ```` ``` ```` / 首尾空白）→ `org.json` 解析
2. 解析失败 → 重试 1 次（指数退避）
3. 仍失败 → 卡片标 `FAILED`，保留原文，用户可手动"重新分析"
4. 网络异常/超时 → 卡片标 `PENDING`，可「一键分析」手动重试

### 6.4 成本
每次手动分析 1 次请求（收藏不产生调用；重复分析已分析卡另计）。model 默认 `deepseek-v4-flash`，可在设置改；请求显式禁用思考模式（v4 默认 high-effort 思考，结构化 JSON 任务收益低、费用高）；「一键分析」并发 5（v7）。

### 6.5 重点词级别补充（本地词表，零 LLM 成本）
- 资产 `assets/word_levels.tsv`：80,440 词 × (级别/英标/美标/释义/四级标志)，级别词 16,241（柯林斯缓存级别标签 + 开源考研/雅思词表）+ 64,199 无级别兜底词（柯林斯缓存全量，仅音标/释义兜底，不参与补缺）。音标**英/美双列**（源 kd_data.db pron 英/美；英 35,124、美 36,546，缺失列留空由 LLM 兜底）。
- 分析时句子分词 → `HashSet` 查表（O(1) 每词，含变形还原）→ 未被 LLM 关键词覆盖的级别词追加为 CardWord（上限 10 个/卡，v7.2 由 5 放宽），并显示级别角标；LLM 关键词本身命中级别表也补角标，未命中则用兜底词补音标/释义。
- **变形还原两层**（等价 GoldenDict 的 Hunspell 形态层，但标准 en_US 词典把不规则变形列为独立词条、规则推导不出 knelt→kneel，故用代码内 IRREGULAR 映射表）：规则变形 `forms()` 推导（-s/-es/-ies/-ed/-ing 含双写去一）+ 不规则映射表（went→go、knelt→kneel、mice→mouse、better→good 等约 250 条）。查词/音标/原型还原共用该两层。
- **简单词过滤（v7 调整）**：LLM 挑出的关键词不再按四级标志剔除——难度判断交给 LLM（prompt 含学习者水平基线：超纲必挑，四级词语境用法值得学仍挑，如 resign oneself to）；四级标志仅用于词表补缺兜底的防刷屏（数据中 3761 个四级基础词全部带考试级别，一刀切会误伤 resign/abandon 等超纲词）。
- **同形异义变形（v7.1）**：词表把变形词单列为独立词义时（felt=专八名词「毡」，句中却是基础词 feel 的过去式），补缺跳过、LLM 关键词也不回退该词条释义/级别——超纲级别与释义属于另一词义，兜底只会带入错义（曾出现 felt→「毡；毡制品」）。判定：原形（变形还原）存在且为四级基础词。
- **无级别词条遮蔽（v7.2）**：柯林斯把变形词单列为无级别兜底词条（ingredients 无级别、原形 ingredient 雅思）时，查表命中即停会让原形考试级别永远查不到、补缺漏词——补缺改为 surface 无级别/未收录时回退变形原形找带级别词条（对齐音标兜底的跳过空词条模式）。另复核 rebellion/rebel 被柯林斯误标四级基础标志，已置 0（考研词仍可补缺）。
- 级别词不带 LLM 语境释义，展示词表释义；音标优先柯林斯 IPA（空串归一为 null，避免空行）。
- v6 起重点词元信息一行展示（详情页/复习页），全部中点号隔开、缺项不显示：`英 /ˈluːmd/ · 美 /luːm/ · 原型 loom · CET6`；标记映射：六级→CET6、专四→TEM4、专八→TEM8、雅思→IELTS、考研→考研；构词独立一行、淡蓝色、按构件渲染：`sneak（词根：偷偷走）＋ -ing（后缀：进行中）`。

### 6.6 术语表（全局，可选）
- 设置中维护多行术语表（`tolerance → 公差`）；分析请求注入 system prompt「以下术语必须按指定译法翻译」，对所有来源生效。按小说/来源分组的术语表列入 v1.5。

## 7. 复习调度（SM-2）

- 四键 → quality：重来=1 / 困难=3 / 良好=4 / 简单=5
- `q≥3`：`reps+1`，间隔 `1 → 6 → ef×interval`；`ef += 0.1-(5-q)*(0.08+(5-q)*0.02)`，下限 1.3
- `q<3`：`reps=0, lapses+1, interval=1`，次日再出
- 每日队列 = 到期卡片（dueAt≤今天）；**新卡不再自动进队**：分享后为「待学习」，详情页点「开始学习」→ 次日到期进入排程（无每日新卡上限）
- 加练独立队列，不触碰上述字段，仅写 ReviewLog(type=1)

## 8. 离线策略

| 场景 | 行为 |
|---|---|
| 采集时断网 | 入库成功（待分析），联网后「一键分析」补 |
| 分析中网络闪断 | 卡片回退 PENDING，可「一键分析」重试 |
| 完全离线使用 | 复习/浏览/统计全可用；分析停摆（手动触发时才请求） |

**v1.5 本地词典**（本期不做，接口预留）：ECDICT 裁剪版 SQLite（几 MB~十几 MB），离线即时词义+音标；词频表做重点词判定（COCA/BNC）。**语境释义永远由 LLM 提供**——词典只做兜底与即时反馈，这是"语境释义"需求（词义消歧）的必然结论。

## 9. 非目标（明确不做）
- 跨平台、登录/云同步、全文翻译、自动无障碍采集（v2）、背单词 App 的"单词为中心"复习
- 不做 Anki 式高密度界面；保持"一屏一句"的阅读伴学感

## 10. 成功指标（个人产品）
- 采集→分析成功率 ≥ 95%（容错链目标）
- 打开 App 到开始复习 ≤ 3 步
- 每日复习流程 5 分钟内可完成

## 11. 已知取舍与坑

### 分享来源为什么一直是「系统分享」（2026-08-31 诊断，保持现状）

- **现象**：Android 11+ 上所有分享卡片的来源都是「系统分享」，v0.2.2 之前正常显示来源 app 名/书名。
- **原因**：`dec22ce`（v0.2.2）把分享接收从 Activity（`MainActivity`，系统会写 referrer/callingPackage，`getReferrer()` 可解析来源包名）换成了后台绑定 Service（`ShareReceiverService`，分享零切换）。系统只为 Activity 启动写 referrer/callingPackage，**Service 收不到**；阅读 app 分享选中文本时通常只带 `EXTRA_TEXT`，不带 `EXTRA_TITLE`/`EXTRA_SUBJECT`，系统也不对 Service 路径注入 `EXTRA_REFERRER_NAME`（AOSP：referrer 仅 fillIn 到走 `startActivityAsCaller` 的直接分享目标），ClipData 无 label → `ShareSource` 解析链全空 → 降级「系统分享」。
- **决定**：保持现状（零切换体验优先，来源信息对产品价值有限）。
- **若未来要恢复来源**，两条路：
  1. 回退 Activity 接收（`ShareReceiverActivity` 全版本启用、停用 Service 入口）——恢复来源但分享时有一次任务切换闪现（即 `dec22ce` 消除的体验）；
  2. Service + `UsageStatsManager` 兜底——分享到达瞬间来源 app 仍在最前台（零切换设计的前提），查最近 foreground 事件拿包名解析来源；需用户开「使用情况访问」特殊权限（设置 → 应用 → 拾句 → 使用情况访问）。

## 12. 发音（v0.6.0 新增）

- **范围**：发音条目 = 重点词（含词组形态）与搭配短语；详情页重点单词行/搭配行、复习页背面重点单词行尾各一个喇叭按钮（共用 `KeywordRow`）。不含整句/例句朗读。
- **引擎**：默认「系统语音」（开箱即用，音质随 ROM）；可选「本地语音引擎」= sherpa-onnx（官方 AAR 1.13.7，仅打包 arm64-v8a，首个 native 依赖）+ kokoro v1.1 int8。设置 = 主设置对话框内「发音设置…」子对话框，改动即时生效（无保存制）。
- **声库**：整包单文件下载（约 147 MB，官方 GitHub release，sha256 钉死 + 断点续传）→ 解压至 `filesDir/tts/packs/`（`commons-compress` 流式解压，完成后删包）。v1 美音声线 2 个（Maple sid0 默认女声 / Sol sid1）；piper 家族接口预留（v1.1 视男声需求）。音色 id 持久化于 Settings（`voice_engine`/`voice_id`）。
- **回退与提示**：本地未就绪（未下载/加载失败）时播放自动回退系统语音，每会话提示一次。
- **缓存**：合成音频按（音色, 文本）落盘 LRU（20 MB），重复播放/离线秒开；删语音包即清缓存。
- **生命周期**：引擎 lazy 单例，首次本地播放加载后常驻（int8 ~110 MB 模型，常驻内存估算 200-300 MB[估计]，仅启用本地引擎后产生）。
- 术语见 `CONTEXT.md`「发音」节；选型取舍见 `docs/adr/0002-local-pronunciation-tts.md`。无 Room 表结构变更。

## 13. 词源与词根库（v0.7.0 新增）

- **资产总览**：`assets/` 五类资源（两个 LLM prompt、词表/词根两个本地数据文件、TTS 语音包清单）的作用与「LLM 搭配」见 `DEVELOPMENT.md` §3.1；本节只记词源相关设计。
- **目标**：给重点词加「词源分析及扩展」——以词根为锚点展示同根词族；统计页新增词根库/高频难点词分页列表。
- **数据源**：ECDICT `wordroot.txt`（`skywind3000/ECDICT`，MIT，~611 条含 root/prefix/suffix）打包为 `assets/wordroot.txt`，启动加载为 `Roots` 三索引（root→entry、word→root 反向、词根词→推导拆解）。零 LLM 成本、离线一致。**词根库只收录 `class=root`（423 条）；前后缀仅作词族拆解构件，不单列。**
- **扩充（v0.9.0）**：以蒋争《英语词汇的奥秘》（自购正版、个人使用）词根/词缀章节并入，仅取事实字段（词根拼写/构词义/例词），例词经 `word_levels.tsv` 收词校验并跳过已在库内归属词；数据集扩至 772 条（root 503、prefix 117、suffix 152），词源入口覆盖词 +1,146（此前无入口的 petition/orient/cultivate/military/certify/fluid/detail 等常见词获得入口）。语义分家的数字消歧条目（`-er1/-er2`、`in-1/in-2`）不并入以避免归属漂移。
- **拆解（前缀+词根+后缀）**：数据集无逐词标注，靠交叉推导——仅当该词同时被数据集某前缀/后缀条目交叉收录、且归一化后 `prefix+root+suffix` 精确等于该词才给出；否则只标「同根」、不虚构（portcullis/portal 等无拆解）。
- **源可信分层（不编造）**：静态事实字段（音标/词性/级别/原型/构词）**本地可信数据优先**（`word_levels.tsv` + `wordroot.txt`），LLM 仅本地无数据时兜底且「不确定→null 禁止编造」。prompt 同步放宽「必须提供读音」→「本地无数据且不确定→null」。
- **专有名词排除**：分析 prompt 新增——人名/地名/建筑物名/机构名/品牌名/书名等专有名词不作为 keyword（避免把 London 拆词根、给 Mary 标词性问题）；句子仍翻译，专名里的普通词成分单独挑出。
- **UI**：词元信息行（详情页+复习页共用 `KeywordRow`）新增「词源 ›」入口（仅当单词命中 word→root 时显示）→ `WordRootScreen`（词根页：header 词根·含义·来源·词族规模 + 词族行=词+拆解/同根+词典义兜底+「你见过 N 次」可点入词频/加练）。统计页「高频难点词」「词根库」改为 `>` 入口 → 全屏分页列表（无限滚动，页大小 30；词根库按词族规模降序 + 顶部搜索）。
- **导航**：全屏覆盖页改造成**栈**（`Overlay` sealed + LIFO），返回键始终关栈顶——解决 card→rootPage、rootPage→word、word→card 三链的顶层循环。
- **词源分析随语句分析 + 支持库落地（v0.8.0）**：
  - **词源分析挂载**：`analyzeCard` 保存关键词后，对命中词根的关键词**异步**触发词源生成（按词根去重、幂等，不阻塞分析完成；失败/离线回退本地拆解+同根）。词根页优先读词源库，缺则触发生成+落地。
  - **支持库（随学习扩充）**：每次分析把 LLM 产出的单词/词组/音标/词性/释义/原型/词缀/级别/词根 **upsert 到 `lexicon`**（词/词组知识库）；词根词源生成后落地 `root_etymology`（词源库：词族每词的 `breakdown[part/type/中文义]` + `derivation` 推导义）。二者跨卡、跨会话累积，逐步形成个人词库。
  - **Room v8**：新增 `lexicon`、`root_etymology` 两表；`AppDatabase.version` 8（迁移 7→8 建两表）。词源数据不再仅靠本地 `wordroot.txt`（作回退/兜底），主视图由 LLM 生成 + 缓存。
  - **词根页 UI（自主设计）**：header=词根名（大字）+ 来源右上（`Latin → Latin（拉丁语）`），构词义另起一行（`meaningZh` 中文优先、缺则英文）；词族行=词（绿）+ 音标（英/美，词表查得才显）+ 拆解（前缀/后缀琥珀、词根绿，各附中文短义「共同/向外/名词」）+ 推导义（`共同携带自身 → 举止；风度`）居右；紧凑、细分割线。词源库缺失时整体回退本地拆解/同根；**LLM 词源漏掉的词族词用本地行补齐**（词族不缩水）。
- **说明**：数据源取舍见 `docs/adr/0003-word-etymology-offline-root-dataset.md`；术语增量见 `CONTEXT.md`（词根库/词源库/支持库）。
